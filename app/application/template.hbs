<body class="site">
  <nav class="nav">
    <div class="column is-10">
      <div class="nav__inner">
        <div class="nav__content">
          <a href="/"><img src="./images/nav/logo.svg" class="nav__logo" alt="Junior Logo" /></a>
        {{#if (and session.currentUser (not session.currentUser.profile.profileCompleted))}}
            {{!-- <h2 class="nav__item">
            {{#link-to 'jobs' class="nav__item"}}Job Board{{/link-to}}
            {{#link-to 'forum' class="nav__item"}}Forum{{/link-to}}
            {{#link-to 'profile' class="nav__item"}}Profile{{/link-to}}
          </h2> --}}
        {{/if}}
        {{!-- {{#link-to 'signup' class="cta-btn"}}Join{{/link-to}} --}}
      </div>
      </div>
    </div>
  </nav>
  <main class="site__content">
    <div class="column is-10">
    <div class="flash-messages">
      {{#each flashMessages.queue as |flash|}}
        {{#flash-message flash=flash class="success-message flash-success" as |message|}}
          <div class="chip">
            {{flash.message}}
          </div>
        {{/flash-message}}
      {{/each}}
    </div>
      {{outlet}}
    </div>
  </main>
  {{!-- <footer class="footer">
    <div class="column is-10">
      <div class="footer__inner">
        <ul class="footer__content">
          <li class="global__h5">© Hello Junior 2016</li>
        </ul>
      </div>
    </div>
  </footer> --}}
</body>
